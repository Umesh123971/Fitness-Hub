{
  "name": "21c-fitness-hub",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "render-build": "npm --prefix frontend ci && npm --prefix frontend run build && npm --prefix backend ci",
    "start-backend": "npm --prefix backend start"
  }
}