{
  "name": "21c-fitness-hub",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "render-build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend install",
    "start-backend": "npm --prefix backend start"
  }
}